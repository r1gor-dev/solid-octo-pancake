import React, {Component} from 'react';

export class Form extends Component {
    render() {
        return (
            <div>
                Add radius form
            </div>
        );
    }
}

export default Form;